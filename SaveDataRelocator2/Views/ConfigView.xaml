<UserControl x:Class="SaveDataRelocator2.Views.ConfigView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="270.184" d:DesignWidth="491.086">
    <StackPanel>
        <Border Height="60" Background="{StaticResource BlueBrush}" Padding="5" CornerRadius="5" Margin="5" VerticalAlignment="Top">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="30*"/>
                    <RowDefinition Height="70*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="Backup Path:" Foreground="White" FontWeight="Bold" />
                <TextBox x:Name="BackupPath" Grid.Row="1" Margin="0,5,0,0" Text="D:\Dropbox\GameSaves\Crank" VerticalContentAlignment="Center" />
            </Grid>
        </Border>
        <Button x:Name="ButtonSave" Margin="5,10,0,0" HorizontalAlignment="Left" Content="Save" Background="{StaticResource BlueBrush}" Foreground="White" FontSize="20" Padding="10,5" BorderBrush="{x:Null}" BorderThickness="0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="5" />
                </Style>
            </Button.Resources>
        </Button>
    </StackPanel>
</UserControl>
